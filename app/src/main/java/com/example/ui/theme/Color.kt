package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Vibrant Palette - Premium Light Theme Color Set
val VibrantBgSlate = Color(0xFFF8FAFC) // Slate 50
val VibrantCardBg = Color(0xFFFFFFFF)  // Pure White surface
val VibrantHeaderBg = Color(0xFFFFFFFF)

val VibrantTextDark = Color(0xFF0F172A)    // Slate 900 for high-contrast titles
val VibrantTextMedium = Color(0xFF475569)  // Slate 600 for body texts
val VibrantTextMuted = Color(0xFF94A3B8)   // Slate 400 for secondary tags
val VibrantBorder = Color(0xFFE2E8F0)      // Slate 200 for clean subtle borders

// Accent Brand Colors
val VibrantTealPrimary = Color(0xFF0D9488)    // Teal 600
val VibrantTealLight = Color(0xFFCCFBF1)      // Teal 100
val VibrantEmerald = Color(0xFF10B981)        // Emerald 500

// Pastel Category Accents
val VibrantVioletBg = Color(0xFFF5F3FF)
val VibrantVioletText = Color(0xFF7C3AED)

val VibrantAmberBg = Color(0xFFFFFBEB)
val VibrantAmberText = Color(0xFFD97706)

val VibrantBlueBg = Color(0xFFEFF6FF)
val VibrantBlueText = Color(0xFF2563EB)

val VibrantRoseBg = Color(0xFFFFF1F2)
val VibrantRoseText = Color(0xFFE11D48)


// --- Compatibility mappings for the Vibrant Palette ---
val SpaceBlack = VibrantBgSlate        // Slate 50 backdrop
val CosmicNavy = VibrantCardBg         // White cards
val SlateDark = VibrantCardBg          // White card surfaces
val SlateLight = VibrantTextMedium     // Slate text
val AccentTeal = VibrantTealPrimary    // Brand Teal primary
val ElectricViolet = VibrantVioletText // Vibrant violet accent text
val SunsetOrange = VibrantAmberText    // Vibrant amber accents

val CardBgNavy = VibrantCardBg         // White card background
val TextPrimary = VibrantTextDark      // Slate 900
val TextSecondary = VibrantTextMedium  // Slate 600
val BorderLight = VibrantBorder        // Slate 200 border



